# https://github.com/ansible-collections/community.general
# Official Doc
ansible-galaxy collection install -r ./requirements.yaml

# Note that if you install the collection manually, it will not be upgraded 
# automatically when you upgrade the Ansible package. To upgrade the collection to the latest available version, run the following command:

ansible-galaxy collection install community.general --upgrade
